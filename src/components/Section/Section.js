import React from 'react';
import { useLocation } from 'react-router-dom';

const Section = ({ dividers }) => {

    switch (dividers.section) {
        case 'Who What When': 
        case 'Perpetrator':
        case 'Reporting':
        case 'Victim':

  }
};

export default Section;


