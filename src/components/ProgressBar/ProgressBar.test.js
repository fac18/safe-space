import React from 'react';
import { render } from '@testing-library/react';
import ProgressBar from './ProgressBar';

test('The ProgressBar renders', () => {
  render(<ProgressBar></ProgressBar>);
});
